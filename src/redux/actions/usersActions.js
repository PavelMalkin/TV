import { createAction } from "@reduxjs/toolkit";

export const deleteContact = createAction('deleteContact')

export const editContact = createAction('editContact')

export const saveChange = createAction('saveChange')

export const addContact = createAction('addContact')

export const addCoordinates = createAction('addCoordinates')